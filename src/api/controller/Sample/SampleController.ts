import { Request, Response } from "express";
import { getRepository } from "typeorm";

/** sample export function */
// export const sample = async (_request:Request, response: Response) => {
//     return response.send(_request.body);
// }
